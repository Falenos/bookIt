var React = require('react');
var Room = require('Room');

var RoomList = React.createClass({
  render: function () {
    return (
      <div>
        <Room />
      </div>
    );
  }
});

module.exports = RoomList;
